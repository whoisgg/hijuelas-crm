/**
 * Drizzle schema placeholder.
 *
 * El modelo de datos completo se define en:
 *   2BGG/02 - Projects/hijuelascrm/Hijuelas CRM - Modelo de Datos.md
 *
 * Este archivo lo llena el agente de schema en una iteración separada.
 * Por ahora exporta solo tipos vacíos para que el cliente Drizzle compile.
 */

export const schema = {} as const;

export type Schema = typeof schema;
